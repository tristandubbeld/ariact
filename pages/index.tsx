import React from 'react';

import { DefaultLayout } from '@/layouts/DefaultLayout';

const IndexPage = () => (
  <DefaultLayout>
    <h1>I have no homepage</h1>
  </DefaultLayout>
);

export default IndexPage;
